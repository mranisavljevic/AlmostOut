{
  "name": "almost-out-firebase",
  "version": "1.0.0",
  "description": "Firebase backend for AlmostOut App",
  "scripts": {
    "deploy": "./deploy.sh",
    "start:emulators": "firebase emulators:start",
    "functions:serve": "npm run --prefix functions serve",
    "functions:deploy": "npm run --prefix functions deploy",
    "setup": "firebase init"
  },
  "keywords": ["firebase", "almost-out", "shopping-list", "mobile-app"],
  "author": "Miles Ranisavljević",
  "license": "MIT",
  "devDependencies": {
    "firebase-tools": "^12.4.0"
  }
}